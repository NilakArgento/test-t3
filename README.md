# Test T3

Simple CLI application for calculating the minimal step based on three input values.


## How to run

Make sure you have Node.js installed.

Run the application:

```bash
node app.js
```

Run the tests
```bash
node --test test.js
```
